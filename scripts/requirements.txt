# Python dependencies for IGC parsing and processing

# Core dependencies
tqdm>=4.60.0
requests>=2.25.0

# PostgreSQL driver (psycopg v3, NOT psycopg2-binary!)
# Code uses: import psycopg
psycopg>=3.0.0

# IGC parsing libraries
libigc>=1.1.0  # Battle-tested IGC parser with thermal/glide detection
aerofiles>=1.0.0  # Aviation file formats library

# Weather data processing
pygrib>=2.1.0  # GRIB file reader for GFS data

# XC score calculation (xc_score is included locally in scripts/xc_score/)
numpy>=1.20.0
scipy>=1.7.0

# Data processing & visualization
pandas>=1.3.0
matplotlib>=3.3.0
pillow>=8.0.0  # PIL for image processing

# Utilities
colorama>=0.4.0
